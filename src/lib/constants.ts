export const PUBLIC_ROUTES = [
  '/', '/login', '/register', '/forgot-password', '/auth/callback',
  '/pricing', '/features', '/privacy', '/terms',
] as const

export const STATIC_ASSET_PATTERN = /\.(png|jpg|jpeg|gif|svg|ico|css|js|woff2?)$/
